import { waves } from "./waves";
import { attacks, releases } from "./envelopes";


export const PRESETS = {
  waves,
  attacks,
  releases,
};


import { ltx } from "#scripts/utils";

const _ = [
    {
      title: "",
      latex: ltx `f\left(t\right) = `,
    },
];
