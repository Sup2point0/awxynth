import type { Octave, Scalar, Seconds } from "#scripts/types";


export const DEFAULTS =
{
  GAIN: 0.7 as Scalar,
  
  OCTAVE: 4 as Octave,

  ATTACK_TIME:  1.0 as Seconds,
  RELEASE_TIME: 1.0 as Seconds,
};
