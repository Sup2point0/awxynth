/**
 * Visual theme settings for the application.
 */
export const Theme = {
  WAVE_OPACITY: 0.1,
};;

/**
 * Theme colours for the application.
 * 
 * For Desmos with inverted colours enabled, use `*_INV` variants.
 */
export enum Colour {
  GREEN = "#40f190", GREEN_INV = "#bf0e6f",
}
